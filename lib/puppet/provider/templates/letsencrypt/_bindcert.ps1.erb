$Cert = Get-ChildItem -Path cert:\LocalMachine\My\<%=resource[:thumbprint]%>
$binding = Get-WebBinding -Name '<%= resource[:name]%>' -Protocol https
$binding.AddSslCertificate($Cert.Thumbprint, 'MY')
